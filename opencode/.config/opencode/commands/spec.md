Phase 1 - Codebase Research: Before asking anything, explore the project to understand:
- Language, framework, project structure
- Existing patterns for similar features
- Test framework and conventions
- Relevant libraries and dependencies already in use

Phase 2 - Requirements: First extract anything I already told you in my command so you don't re-ask it. Then ask me targeted questions — but skip anything the codebase already answers (e.g., don't ask what test framework to use if you can read package.json). One question at a time. Cover:
- Exactly what the feature should do (if unclear from my description)
- Edge cases and error scenarios
- Integration points with existing code
- Any constraints I haven't mentioned

Phase 3 - Plan: Once I confirm requirements are complete, produce a detailed implementation plan with:
- Files to create or modify
- Step-by-step implementation order
- Testing approach
- Potential risks or dependencies

No code. Plan mode format only.
